tectonic

# Student PII — never commit. Roster lives in website/students.json (id+name only is safe).
*.xlsx
*.xls
*.csv

# Answer-key source banks for both courses — kept on the instructor's
# machine but never committed (the markdown lists answers in a
# human-readable table).
website-CST/MCQ Bank*.md
MCQ-Question-Bank.md
MCQ-Sheet.md

__pycache__/
*.pyc
.DS_Store
.idea/
.vscode/
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
